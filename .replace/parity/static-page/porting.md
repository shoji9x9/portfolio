# 移植メモ（porting）

- 対象 slug: `static-page`
- モード: feature
- 最終更新: 2026-07-29T06:05:00+09:00
- 対象 target: `local-dev`（`http://localhost:5173`）
- 現行コードの参照元: `shoji9x9/shoji9x9.github.io` の `b10b54a…`（`app/page.tsx`・`components/**`・`services/**`）

## ページ分割とフェーズ進捗

| ページ | フェーズ状態 | 備考                                                                                                    |
| ------ | ------------ | ------------------------------------------------------------------------------------------------------- |
| `/`    | 新側 green   | 対象ページが 1 つのためフェーズ分割は行わない（Issue #22 の方針）。書き込み経路は現行に無く読み取りのみ |

## 確信度の申告（実装単位ごと 1 行・常に書く）

| 実装単位                                        | 確信度 | 根拠・懸念                                                                                                                                                                                                                                                                                               |
| ----------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/App.tsx`（ページ構成・セクション順）       | 高     | 現行 `app/page.tsx` のセクション順・クラスをそのまま移植。実 DOM とも照合済み                                                                                                                                                                                                                            |
| `src/components/portfolio/ProfileTable.tsx`     | 高     | 現行のテーブル構造・クラス（`table-fixed` / `w-1/4` / `p-1` / 罫線）を 1:1 で移植                                                                                                                                                                                                                        |
| `src/components/portfolio/BadgeImage.tsx`       | 高     | 現行 `Badge` は `<img alt src>` のみ。`mr-2 h-5` も同一                                                                                                                                                                                                                                                  |
| `src/components/portfolio/CareerSection.tsx`    | 中     | 見出しレベル・余白は現行のベースライン実測値（h4 mb-16px / カード p-16px / w-768px）と突き合わせて決めた。現行 `Heading` はクラス結合の副作用で `mb-2` と `mb-4` が同時に付き、Tailwind の出力順で後者が勝っていた。**実測値に合わせたので値は一致するが、現行の意図がどちらだったかは読み取れていない** |
| `src/components/portfolio/ArtifactSection.tsx`  | 中     | 同上（カード構造は `ProjectCard` と同型）                                                                                                                                                                                                                                                                |
| `src/components/portfolio/Qualifications.tsx`   | 高     | 入れ子リストの構造・`pl-4` を現行どおり                                                                                                                                                                                                                                                                  |
| `src/components/portfolio/TextLink.tsx`         | 高     | 現行の `text-blue-500 hover:text-blue-700 underline` をトークン経由で再現                                                                                                                                                                                                                                |
| `src/components/portfolio/PortfolioSection.tsx` | 中     | 現行は role を持たない `div`。新側は `section` ＋ `aria-labelledby` にした（アクセシビリティの改善）。**構造が変わるため意図的差異にあたるが、`may_change`「HTML の id/name とコンポーネント内部構造」の範囲と判断した**                                                                                 |
| `src/index.css`（design token）                 | 中     | 背景・前景・リンク色・罫線色は現行の実測値／Tailwind v3 のパレット値を固定した。**現行が使っていなかったトークン（muted / primary 等）は削除**しており、将来 shadcn の部品を戻すときは再定義が要る                                                                                                       |
| `src/data/types.ts`（新側スキーマ）             | 高     | 写像規則は `.replace/references/static-data-semantics.md` に明文化し、フェーズ B の逆写像検証で担保                                                                                                                                                                                                      |
| `seed/phase-b.ts`（フェーズ B 写像）            | 高     | 逆写像して論理データと突き合わせる検証付き。意図的差異はレジストリ宣言済みのものだけ適用                                                                                                                                                                                                                 |
| `e2e/parity/lib/locator-map/static-page.new.ts` | 高     | 例外はコンテナー解決 4 つのみ。新側 42 件 green で確認済み                                                                                                                                                                                                                                               |

## 未解決として明示した箇所（TODO 一覧）

| 箇所 | 未解決の内容 | 対応                                                                                   |
| ---- | ------------ | -------------------------------------------------------------------------------------- |
| なし | —            | 推測で実装した箇所は無い。判断が要った点はすべて上表の確信度「中」として理由を明記した |

## 現側脆弱マッピングの不要化確認結果

| 現側の脆弱箇所                                          | 新側で不要になったか | 記録                                                                                                    |
| ------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------- |
| セクションの解決（`main > div` を h2 でフィルタ）       | **不要になった**     | 新側は `section` ＋ `aria-labelledby`（role=region）で `getByRole("region", { name })` に置き換わった   |
| 職務経歴プロジェクトカードの解決（`> div > div > div`） | **不要になった**     | 新側は `article` ＋ `aria-labelledby`（role=article）で `getByRole("article", { name })` に置き換わった |
| 製作物カードの解決（`> div > div`）                     | **不要になった**     | 同上                                                                                                    |

現側の 3 箇所はいずれも DOM 構造依存だったが、新側では role ＋アクセシブルネームで引ける。
**新側マッピング（`static-page.new.ts`）は書いたが、中身は「構造依存をやめて role で引く」だけ**で、
セクション・カードの内側の要素は `portable.ts` の記述がそのまま両実装に解決している（例外ゼロ）。

## スイートに入れた変更（穴埋め）と強度ゲート再実行

新側を green にする過程で、`parity-suite` が書いた assertion のうち 3 箇所が
**新側の正当な改善を赤にしていた**ため緩めた。assertion を変えたので強度ゲートを再実行し、
14 故障すべての捕捉と対照の清潔さを再確認済み（[`strength.md`](strength.md)）。

| 変更箇所                         | 変更前                                       | 変更後                                                     | 理由                                                                                                                                                                                                                 |
| -------------------------------- | -------------------------------------------- | ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `checkPageBasicsAndSectionOrder` | h2 見出し 9 件（LAPRAS を含む）の完全一致    | 期待列から LAPRAS だけを外す（引き方・一致条件は据え置き） | 同じページに同居する機能 `lapras`（Issue #23）を要求すると `static-page` を単独で green にできない。**判定の強さは落とさない**——初回は textContent・前方一致まで緩めてしまい、敵対的レビューの指摘（M4）で元に戻した |
| `checkAriaCareers`               | セクション直下に h4/h5 が並ぶ前提            | h2 ＋ 会社の h3 まで                                       | カードを `article` で包むと入れ子の深さが変わる。カード内部は `checkAriaCareerCard` が担う                                                                                                                           |
| `checkTabOrder`                  | アカウントバッジ名をデータセットから直接取得 | 意図的差異層経由で side ごとに解決                         | 宣言済みの GitHub 綴り差異の適用漏れ（実装ミス）                                                                                                                                                                     |

あわせて `playwright.config.ts` の `new` プロジェクトに `testIgnore` を追加し、現側専用スペック
（ベースライン採取・強度ゲート）が新側で走らないようにした。新側で走らせると現側の証跡を
上書きしてしまうため。

## 骨格の整理（Issue #4 由来）

現行 `/` にヘッダー・ナビ・フッター・ボタンが無いため、Issue #4 の骨格のうち placeholder 専用
だったものを削除した（ユーザー判断）。復元は `git show bf819a8:<path>`。

| 削除                                                                              | 理由                                                                                                            |
| --------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `src/components/layout/AppShell.tsx` / `src/lib/date.ts`                          | `/` に共通レイアウトを適用しない方針。予定されている 2 ページ目が無く、残すと未使用コードと knip 除外登録が残る |
| `src/lib/repository.ts` / `src/components/ui/button.tsx`                          | placeholder の「GitHub で確認する」ボタン専用。現行 `/` にボタンは無い                                          |
| `lucide-react` / `@base-ui/react` / `class-variance-authority` / `tw-animate-css` | 上記の部品が使っていた依存                                                                                      |

`components.json`（shadcn の設定）は残している。将来 shadcn の部品を戻すときの設定として使うが、
戻すときは `@base-ui/react` と `lucide-react` の再導入が要る。

## 敵対的レビューで直したもの

詳細は [`review.md`](review.md)。指摘 12 件のうち 6 件を修正した。とくに 2 件は実装・検証の穴だった。

| 指摘                                                                              | 直した内容                                                                                                                       |
| --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| フェーズ B の現新一致検証が実質 `map∘unmap = id` で、宣言外の正規化を足しても通る | 往復をやめ、**差のある箇所を列挙して宣言済み一覧と完全一致するか**を見る形へ書き換えた。宣言外の差を足すと落ちることを実測で確認 |
| セクション順チェックを必要以上に緩めていた                                        | 元の強さへ戻した。その結果 **`STATIC_PAGE_SECTIONS` の並びが現行と違っていた**（資格と製作物が逆）ことが判明し修正した           |
| 資格分類の安定 ID が一意性・形式の検証を受けていない                              | フェーズ B の検証に追加                                                                                                          |
| 意図的差異の表が写像側とスイート側で二重化                                        | 写像側の表が動いたら落ちる独立したテストを `seed/phase-b.test.ts` に追加                                                         |
| `App.test.tsx` が画像 URL・技術スタックの描画を見ていない                         | alt と src の対応、技術スタックの出現回数まで検査するようにした                                                                  |

**レビュー後にユーザー判断で追加対応したもの**（2026-07-29）:

| 指摘                                          | 対応                                                                                                                                                                                   |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `resolveTechStack` の失敗でページ全体が落ちる | 例外送出をやめ、該当バッジだけ落として `console.warn` を出す形にした                                                                                                                   |
| フォント差                                    | 原因を特定（版差ではなくヒンティング命令 `prep` の有無が主因）。現行と同じ Inter 3.019 のヒンティング無しビルド（`inter-ui@3.19.3`）へ差し替え、実測で幅が現行と一致することを確認した |
| LAPRAS セクションを守る assertion が無い      | `e2e/parity/page-composition.spec.ts` を追加。`/` に載る機能のセクション一覧を持ち、未実装分は `test.fixme` で保留報告に出し続ける                                                     |

## 差し戻し履歴（parity-diff との往復）

| target | 反復 | 入力 | 再開したフェーズ | 対応                                |
| ------ | ---- | ---- | ---------------- | ----------------------------------- |
| —      | 0    | —    | —                | まだ `parity-diff` を実行していない |
