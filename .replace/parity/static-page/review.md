# レビュー記録（review）

- 対象 slug: `static-page`
- 実施日時: 2026-07-28T22:00:00+09:00
- 方式: 実装役とレビュー役を分離。レビュー役には**未コミット差分のテキストのみ**を渡し、
  実装意図・設計判断・確信度の申告は渡していない（`porting.md` も渡していない）
- 実施場所: ローカルの未コミット差分（PR のレビュー機能上ではない）

## レビュー対象（差分の範囲）

| 範囲                                                  | 内容                                                                                                                                                |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `git diff --staged` ＋ `git diff` ＋ 新規ファイル全文 | 2,098 行。`/` の実装、design token、新側データスキーマとフェーズ B 写像、新側ロケータマッピング、スイートの穴埋め、骨格部品の削除、品質チェック設定 |

レビュー役へは併せて現行アプリのチェックアウト先（`app/page.tsx` / `components/**` / `services/**` /
`app/globals.css` / `tailwind.config.ts`）と、意図的差異レジストリの `keep` / `may_change` を渡した。

## ラウンドごとの指摘と対応

### ラウンド 1（指摘 12 件）

| 重大度 | 指摘（動かない理由）                                                                                                                                                                                                                                            | 対応                                                                                                                                                                                                                                               | 結果                    |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| H1     | LAPRAS セクションが新側に存在せず、それを要求する assertion も同じ差分で削除された。Issue #23 が忘れられても誰も赤くならない                                                                                                                                    | **未修正（記録）**。LAPRAS は機能 `lapras` の担当範囲で `static-page` のスコープ外。ただし「今は誰も守っていない」ことは事実なので `gaps.md` へ明記した                                                                                            | 記録で合意              |
| H2     | 意図的差異の表が写像側（`seed/phase-b.ts`）とスイート側（`e2e/parity/lib/intentional-diffs.ts`）に逐語複製され、片方を変えても E2E を回すまで気づけない                                                                                                         | **修正**。`seed/phase-b.test.ts` に固定値の期待表を独立に置き、写像側の表が動いたら速いテストで落ちるようにした。2 箇所に置くこと自体は「期待値を実装から独立させる」ため意図的に残した                                                            | 解消                    |
| H3     | `verifyNewSideMatchesLogical` が前方写像と同じ表で逆写像するため実質 `map∘unmap = id`。宣言外の正規化（例 `csharp: "C#"`）を足しても検証が通る。さらに逆写像の探索先が `account → framework` のみで `language` を含まず、検出可否がバッジの所属群に依存していた | **修正**。往復をやめ、**新側と論理データの差がある箇所を列挙して宣言済み一覧（`DECLARED_DIFFS`）と完全一致するか**を見る形へ書き換えた。宣言外の差は 1 件でも失敗する。指摘どおり `csharp: "C#"` を足すと落ちることを実測で確認                    | 解消                    |
| M1     | 資格分類の安定 ID が一意性・形式の検証をどこも受けておらず、重複しても全テストが通る                                                                                                                                                                            | **修正**。フェーズ B の検証に一意性と `^[a-z0-9-]+$` の検査を追加                                                                                                                                                                                  | 解消                    |
| M2     | `resolveTechStack` の例外で `/` 全体が真っ白になる。現行は壊れた画像 1 個で済んでいた（可用性の劣化方向）                                                                                                                                                       | **未修正（記録）**。データは生成＋検証済みで、この例外は手編集時にしか出ない。ただし失敗の仕方が現行と変わるのは事実なので `intentional_diffs.pending` へ追記しユーザー判断へ回した                                                                | 保留                    |
| M3     | フォント差で latin グリフの送りが約 1.2% 広く、プロフィール表幅が 511.66px → 515.66px（実測）。画素差分 0.43%。`next/font` の Inter と `@fontsource-variable/inter` はメトリクスが一致していない                                                                | **未修正（記録）**。レイアウトの高さ・位置は完全一致で構造差は無い。原因（opsz 軸・バージョン差・フォールバック指定）は未特定。`parity-diff` の画素経路で必ず上がるため `gaps.md` へ記録し、そこで分類する                                         | 保留                    |
| M4     | セクション順チェックの緩め方が必要範囲を超えていた（アクセシブルネーム → `textContent`、完全一致 → 全件前方一致）。`aria-label` の付け替えや「製作物」→「製作物アーカイブ」が緑のまま通る                                                                       | **修正**。期待列から LAPRAS を外すだけに留め、アクセシブルネームで引く／プロフィール以外は完全一致、を元に戻した。**この修正で実際の不具合を 1 件検出**——`STATIC_PAGE_SECTIONS` の並びが現行と違い（資格と製作物が逆）、緩い判定では素通りしていた | 解消（+ 別件 1 件検出） |
| L1     | `App.test.tsx` がバッジの `imageSrc` を一切見ておらず、技術スタックの描画も保有スキル欄の既出 alt で通ってしまう。メンバー数も片側だけ検査                                                                                                                      | **修正**。alt と src の対応・リンクと画像の対応をマークアップ単位で検査し、技術スタックは**出現回数**で数えるようにした。メンバー数は両側＋ロールタスク＋コメントも検査                                                                            | 解消                    |
| L2     | `phase-b.test.ts` の「現行と新側に同じ論理データ」はディスク上の JSON をどちらも読んでいない                                                                                                                                                                    | **解消（H3 の書き換えに含む）**。検証が値の突き合わせになり、テスト名と実体が一致した                                                                                                                                                              | 解消                    |
| L3     | `expectEqual` が `JSON.stringify` の文字列比較でキー順に依存する                                                                                                                                                                                                | **未修正（記録）**。写像関数のプロパティ並べ替えで偽陽性になりうるが、生成物のキー順は仕様（書式固定）なので順序依存は許容範囲と判断した                                                                                                           | 記録                    |
| L4     | React key に表示文字列を使用（現行は index キー）                                                                                                                                                                                                               | **未修正（記録）**。現行データに重複が無いことをレビュー役が確認済み。重複が入れば `golden-dataset` の安定 ID 検証か描画で顕在化する                                                                                                               | 記録                    |
| —      | 現行との突合（件数・文言・リンク先・見出しクラス・カード幅・色トークン・空 techStack・任意フィールド）は**問題なし**                                                                                                                                            | —                                                                                                                                                                                                                                                  | —                       |

### ラウンド 2

修正後に現側 58 件・新側 42 件を再実行し、いずれも green。強度ゲートも再実行して 14 故障すべての
捕捉と対照の清潔さを確認した。**再レビューは実施していない**（下記「最終判定」の備考を参照）。

## 最終判定

- 判定: **要ユーザー判断が残る**
- 解消した指摘: 7 件（H2・H3・M1・M2・M4・L1・L2）
- 記録に留めた指摘: 5 件（H1・M3・L3・L4、および H2 の残存する二重化）
- ユーザー判断が要るもの:
  - **M3**: フォント差への対応方針（現行の woff2 を vendoring／5.0.19 固定＋`geometricPrecision`／系統差として宣言）
- 備考:
  - レビュー役は差分の読解に加えて**両側でパリティスイートを実行し、画素差分と computed style を
    実測**している。指摘のうち M3 の数値・M4 の失敗シナリオは実測に基づく。
  - **再レビュー（ラウンド 2 のレビュー役への差し戻し）は行っていない。** 修正はすべて指摘に対する
    直接の対応で、新しい設計判断を含まない。ただし「指摘が尽きた」ことをレビュー役に確認させて
    いないので、`parity-diff` の往復でコード変更が入る場合はそこで再レビューする。
