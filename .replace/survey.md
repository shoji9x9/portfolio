# 測定レポート（survey）

- 測定日時: 2026-07-28T17:25:07+09:00（セマンティクス測定の再測定。項目 2〜5 は 2026-07-26T13:51:15+09:00 の測定値を維持）
- 対象環境: `current-prod`（<https://shoji9x9.github.io/>）
- 対象コミット: `b10b54a489fbac94f7bf8beef1a005ffe19ee791`
- 測定エージェント・モデル・エフォート・スキルバージョン: Claude Code / Opus 5 (1M context) / 標準 /
  replace-strategy v1.26.1

再測定の範囲: 項目 1（セマンティクス測定）のみを再実行し、前回と同一の値を得た。項目 2〜5 は現行リポジトリの
対象コミットが変わっておらず、現行サイトも同じ静的成果物を配信しているため、前回の実測値をそのまま維持する
（再測定していない項目を「今回測った」とは書かない）。

## 1. セマンティクス測定（role プローブ）

現行アプリは公開された単一ページのみで、フォームとダイアログを持たない。そのため、一覧系・フォーム系・
ダイアログを含む 3 ページの要件は適用できない。`/` を代表ページとして測定した。

| ページ | 対象要素数 | named | ratio | (role, name) 重複 | 備考                                            |
| ------ | ---------: | ----: | ----: | ----------------: | ----------------------------------------------- |
| `/`    |        134 |   128 | 0.955 |             26 組 | heading 58/58、img 59/59、table 1/1、link 10/16 |

- 測定方法: 同梱の `role-probe.mjs` の `roleProbe` 関数リテラルを chrome-devtools MCP の `evaluate_script` へ渡して
  `https://shoji9x9.github.io/` に対して実行した。画面の console error / warn は 0 件だった。
- unnamed の代表例: アカウント・バッジの 5 リンク（`main > div:nth-child(2) > div:nth-child(2) > a:nth-child(1..5)`）と
  LAPRAS プレビューの 1 リンク（`main > div:nth-child(9) > div:nth-child(2) > a:nth-child(1)`）。いずれも画像だけを子に持ち、
  リンク自身にアクセシブルネームがない。
- 重複の代表例: `期間`（6）、`ロールとタスク`（6）、`メンバー数`（6）、`技術スタック`（9）、`URL`（3）、
  `リポジトリー`（3）、`記事`（3）の見出しと、技術スタックのバッジ画像（`TypeScript` 5、`React` 5、`JavaScript` 4、
  `OutSystems` 4 ほか）。
- 判断: role + accessible name を原則とする。重複 26 組と名前のない 6 リンクには、`parity-suite` でページ固有の
  ロケータマッピングを追加する。フォーム・ダイアログに相当する現行ページはないため、その状態のパリティは対象外とする。
- 前回（2026-07-26）との差分: なし。総数・named・ratio・重複組数・role 別内訳・unnamed の 6 件すべてが一致した。

## 2. DB 復元可否

- 結果: 対象なし（現行は DB を持たない静的サイト）。
- 手段: 該当なし。
- 実測手順: 現行リポジトリの全 28 ファイルと公開ページのネットワーク通信を確認し、DB 接続・DB API・書き込みフォームを
  検出しなかった。
- 復元所要時間: 該当なし。
- 判断: 書き込み側パリティスイートおよび DB スナップショット復元は不要。データは `services/*.ts` の静的配列・
  オブジェクトとして扱う。これを設定の `dataset_mode: static` として宣言している。

## 3. 現行コードの入手性

| 対象           | 入手性 | ソースか | 備考                                                                                 |
| -------------- | ------ | -------- | ------------------------------------------------------------------------------------ |
| バックエンド   | なし   | 該当なし | API・DB・サーバー処理がない                                                          |
| フロントエンド | あり   | ソース   | `shoji9x9/shoji9x9.github.io` の `master`。Next.js App Router の単一ページ           |
| ビルド時処理   | あり   | ソース   | `app/page.tsx` の `getPreview()` が `LINK_PREVIEW_API_KEY` で linkpreview.net を GET |

- 判断: 新側はフロントエンドをパリティスイートと視覚ベースラインで移植する。直接移植するバックエンドはない。

## 4. 副作用の棚卸し

| 副作用                                       | 所有者                            | 検証対象か      | 根拠   |
| -------------------------------------------- | --------------------------------- | --------------- | ------ |
| linkpreview.net への LAPRAS プレビュー GET   | `lapras`                          | スコープ外→gaps | コード |
| Shields.io・AtCoder・LAPRAS media の画像 GET | `static-page`（バッジ）／`lapras` | 対象            | 実測   |

- linkpreview.net への GET: `app/page.tsx` のビルド時 `fetch` であり、秘密鍵が必要なため、現行を変更せずに同一
  リクエストを捕捉できない。
- 画像 GET: 公開ページのネットワークで 27 件の画像 GET を確認しており、新側は画像 URL と描画結果を比較する。
- 現行のページにはデータ作成・更新・削除、メール送信、通知、ファイル生成、バッチ処理はない。

## 5. 既存テストの有無・密度・実装非依存性

- 有無・件数: なし。現行リポジトリのツリーで `test`、`tests`、`__tests__` ディレクトリおよび `*.test.*`、`*.spec.*` を
  検索し、0 件だった。
- 実装非依存（HTTP・DOM 経由）で新旧両方に当てられるか: 該当なし。再利用できる既存 E2E テストはない。
- 実行確認: 該当なし。

## 未測定の項目

- フォーム・ダイアログ: 現行アプリに該当ページまたはコンポーネントがない。
- LAPRAS プレビュー API の生レスポンス: `LINK_PREVIEW_API_KEY` が必要であり、現行アプリを変更せずにビルド時
  リクエストを捕捉することはできない。
- 狭いビューポートのレイアウト: 後続の `parity-suite` で代表 viewport ごとにベースラインを採取する。
- 項目 2〜5 の再測定: 今回は実施していない（対象コミットが不変のため前回値を維持）。現行リポジトリのコミットが
  動いたら再測定する。
