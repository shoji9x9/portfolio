# 測定レポート（survey）

- 測定日時: 2026-07-26T13:51:15+09:00
- 対象環境: <https://shoji9x9.github.io/>
- 対象コミット: `b10b54a489fbac94f7bf8beef1a005ffe19ee791`
- 測定エージェント・モデル・エフォート・スキルバージョン: GitHub Copilot CLI /
  GPT-5.6 Terra / 標準 / replace-strategy

## 1. セマンティクス測定（role プローブ）

現行アプリは公開された単一ページのみで、フォームとダイアログを持たない。そのため、一覧系・フォーム系・ダイアログを含む 3
ページの要件は適用できない。`/` を代表ページとして測定した。

- `/`: 対象要素数 134、named 128、ratio 0.955、`(role, name)` 重複 26 組。
  - 内訳: heading 58/58、img 59/59、table 1/1、link 10/16。

- 測定方法: 付属の `role-probe.mjs` を chrome-devtools MCP で
  `https://shoji9x9.github.io/` に対して実行。画面の console error / warn は 0 件だった。
- unnamed の代表例: アカウント・バッジの 5 リンクと LAPRAS プレビューの 1 リンク。いずれも画像だけを子に持ち、リンク自身にアクセシブルネームがない。
- 重複の代表例: `期間`、`ロールとタスク`、`メンバー数`、`技術スタック`、`URL`、`リポジトリー`、`記事` の見出し、および技術スタックのバッジ画像。
- 判断: role + accessible name を原則とする。重複 26 組と名前のない 6 リンクには、`parity-suite` でページ固有のロケータマッピングを追加する。フォーム・ダイアログに相当する現行ページはないため、その状態のパリティは対象外とする。

## 2. DB 復元可否

- 結果: 対象なし（現行は DB を持たない静的サイト）。
- 手段: 該当なし。
- 実測手順: 現行リポジトリの全 28 ファイルと公開ページのネットワーク通信を確認し、DB 接続・DB API・書き込みフォームを検出しなかった。
- 復元所要時間: 該当なし。
- 判断: 書き込み側パリティスイートおよび DB スナップショット復元は不要。データは `services/*.ts` の静的配列・オブジェクトとして扱う。

## 3. 現行コードの入手性

- バックエンド: 入手性なし（該当なし）。API・DB・サーバー処理はない。
- フロントエンド: 入手性あり（ソース）。`shoji9x9/shoji9x9.github.io` の `master` にある Next.js App
  Router の単一ページ。
- ビルド時処理: 入手性あり（ソース）。`app/page.tsx` の `getPreview()` が `LINK_PREVIEW_API_KEY` を用いて
  linkpreview.net を GET する。

- 判断: 新側はフロントエンドをパリティスイートと視覚ベースラインで移植する。直接移植するバックエンドはない。

## 4. 副作用の棚卸し

- linkpreview.net への LAPRAS プレビュー GET: 所有者は LAPRAS。スコープ外→gaps。`app/page.tsx` のビルド時
  `fetch` であり、秘密鍵が必要なため、現行を変更せずに同一リクエストを捕捉できない。
- Shields.io、AtCoder、LAPRAS media の画像 GET: 所有者はアカウント、保有スキル、LAPRAS。検証対象。公開ページの
  ネットワークで 27 件の画像 GET を確認しており、新側は画像 URL と描画結果を比較する。

現行のページにはデータ作成・更新・削除、メール送信、通知、ファイル生成、バッチ処理はない。

## 5. 既存テストの有無・密度・実装非依存性

- 有無・件数: なし。現行リポジトリのツリーで `test`、`tests`、`__tests__` ディレクトリおよび `*.test.*`、`*.spec.*` を検索し、0 件だった。
- 実装非依存（HTTP・DOM 経由）で新旧両方に当てられるか: 該当なし。再利用できる既存 E2E テストはない。
- 実行確認: 該当なし。

## 未測定の項目

- フォーム・ダイアログ: 現行アプリに該当ページまたはコンポーネントがない。
- LAPRAS プレビュー API の生レスポンス: `LINK_PREVIEW_API_KEY` が必要であり、現行アプリを変更せずにビルド時リクエストを捕捉することはできない。
- 狭いビューポートのレイアウト: 後続の各 `parity-suite` で代表 viewport ごとにベースラインを採取する。
