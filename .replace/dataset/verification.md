# 検証レポート（verification）

- version: 4
- mode: `static`
- 最終更新: 2026-08-07T09:10:00+09:00

## フェーズ A（正本フェーズ）

version 3 で論理データの役割が「現行の忠実な再現」から「ポートフォリオの正本」へ変わった
（[`../references/static-data-semantics.md`](../references/static-data-semantics.md)）。
そのため「現行ソースとの照合」「現行実 DOM との照合」は version 2 までの検査であり、
version 3 以降は**実施していない**（現行サイトを追従させないため、実施しても不一致になるのが正しい）。

### 整合性（version 4 / 2026-08-07）

| 検査         | 結果                                                                                                                            |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| 安定 ID      | 55 個の ID が一意で、`^[a-z0-9-]+$` に一致（`seed/golden-dataset.ts` の `verifyGoldenDataset`）                                 |
| 件数         | profile 4、account badges 5、language badges 7、framework badges 24、careers 2、projects 10、artifacts 3、自己 PR 4、資格分類 4 |
| 形式妥当性   | 生成した 6 ファイルがすべて JSON として読み戻せ、末尾が改行で終わる（`seed/golden-dataset.test.ts`）                            |
| 参照整合     | 外部キーは存在しない。技術スタックの表示名はすべてバッジへ解決できる（解決できなければ写像が失敗する）                          |
| 決定性       | `pnpm exec tsx seed/golden-dataset.ts` が固定 fingerprint `978a9784` を出力（version 3 は `f1aba402`）                          |
| 冪等性       | 連続 2 回実行して生成物・fingerprint が一致（削除 → 生成のため実行前の状態に依らない）                                          |
| 書き込み範囲 | 生成・削除はすべて `seed/data/` 配下。配下外を指すパスはツールが例外で停止する                                                  |

### 整合性（version 2 まで / 現行の再現だった時期）

| 検査                | 結果                                                                                                                                                                                                                                                         |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 現行ソースとの照合  | `services/badges.ts`、`services/careers.ts`、`services/artifacts.ts`、`app/page.tsx` をコミット `b10b54a…` で読み取り、値と定義順を転記                                                                                                                      |
| 安定 ID             | 41 個の ID が一意で、`^[a-z0-9-]+$` に一致                                                                                                                                                                                                                   |
| 件数                | profile 4、account badges 5、language badges 7、framework badges 14、careers 2、projects 6、artifacts 3、自己 PR 4、資格分類 4                                                                                                                               |
| 決定性              | fingerprint `f4c8d11a`                                                                                                                                                                                                                                       |
| 現行実 DOM との照合 | `current-prod`（<https://shoji9x9.github.io/>）を Playwright で読み取り、プロフィール表・自己 PR・資格・希望条件・職務経歴 6 プロジェクト・製作物 3 件・バッジ 26 件の文言／URL／表示順を全件照合。version 2 で GitHub 表記 3 箇所を実値へ戻し、残りは全一致 |

### fingerprint の算出アルゴリズム

生成物を相対パス順に並べ、パスと内容を NUL（`\0`）で連結し、各エントリーも NUL で連結した文字列に
32-bit FNV-1a を適用して 16 進 8 桁で表す。区切りに NUL を使うのは、パス・内容のどちらにも現れない文字を
選んで連結の曖昧さ（別の組み合わせが同じ文字列になること）を防ぐため。内容が同じでもファイル構成が
変われば別の値になる。

### カバレッジ

| 機能          | 含んだパターン                                                                                                                                     |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `static-page` | リンクの有無、外部画像 URL、現行の表示順、複数社・複数プロジェクト、空配列、任意コメント、人数の幅、改行を含む長文、ネストした資格分類、外部リンク |
| `lapras`      | 公開 URL と未検証 gap                                                                                                                              |

### 生成物

| ファイル              | 内容                                                          |
| --------------------- | ------------------------------------------------------------- |
| `profile.json`        | プロフィール 4 件                                             |
| `badges.json`         | アカウント 5・言語 7・フレームワーク等 24 のバッジ            |
| `careers.json`        | 職務経歴 2 社・10 プロジェクト                                |
| `artifacts.json`      | 製作物 3 件                                                   |
| `static-content.json` | 自己 PR 4・資格分類 4・希望条件 1                             |
| `lapras.json`         | LAPRAS の公開 URL と、プレビュー本文を収録しない旨の gap 表記 |

## フェーズ B（新側フェーズ）

フェーズ B で version は変更しない。`dataset_mode: static` では全 target が投入対象となるため、データセット
バージョンの一致は免除されない（`local-dev` / `local-production` / `preview` / `production` のそれぞれで記録する）。

### `static-page` × `local-dev`（2026-07-28）

- 写像の実装: `seed/phase-b.ts`。生成先は `src/data/generated/`（`dataset_static_paths` の `src/data/` 配下）
- 写像規則・意味論差の正本: [`../references/static-data-semantics.md`](../references/static-data-semantics.md)
- 新側スキーマ: `src/data/types.ts`（`parity-replace` が実装）

| 検査                                            | 結果                                                                                                                                                                                                                           |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 現新一致                                        | **新側と論理データの差がある箇所を列挙し、宣言済みの意図的差異 3 件と完全一致**することを確認。宣言外の差は 1 件でも失敗する実装にしてある（逆写像の往復にしない——同じ表で戻すと `map∘unmap = id` になり検証が空回りするため） |
| 説明できない不一致                              | 0 件                                                                                                                                                                                                                           |
| 表現形式の変換                                  | 技術スタック参照（表示名 → 安定 ID）・資格分類（オブジェクト → 配列）・希望条件（2 フィールド → オブジェクト）を逆写像して論理データと一致することを確認                                                                       |
| 安定 ID                                         | 資格分類 4 件の ID が一意で `^[a-z0-9-]+$` に一致（フェーズ A の ID 検証の対象外だったため、フェーズ B 側で検査する）                                                                                                          |
| 冪等性                                          | 連続実行で生成物が一致（削除 → 生成のため実行前の状態に依らない）                                                                                                                                                              |
| 書き込み範囲                                    | 生成・削除はすべて `src/data/generated/` 配下。配下外を指すパスはツールが例外で停止する                                                                                                                                        |
| 再生成し忘れの検出                              | `seed/phase-b.test.ts` がディスク上の生成物と写像結果を突き合わせる                                                                                                                                                            |
| target が実際にゴールデンデータを配信しているか | `local-dev`（<http://localhost:5173>）に対しパリティスイート 42 件が green                                                                                                                                                     |

### `static-page` / `lapras` × `local-dev`（2026-08-04 / version 3）

version 3 の論理データを同じ写像規則で再生成し、`local-dev` に対して検証した。写像規則・宣言済み
意図的差異（3 件）は version 2 から変えていない。

| 検査                                            | 結果                                                                                                                                                          |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 現新一致                                        | `pnpm exec tsx seed/phase-b.ts` が `declaredDiffs: 3` で成功。宣言外の差 0 件                                                                                 |
| 再生成し忘れの検出                              | `pnpm test`（vitest 79 件）が green。`seed/phase-b.test.ts` がディスク上の生成物と写像結果の一致を確認                                                        |
| target が実際にゴールデンデータを配信しているか | `local-dev`（<http://localhost:5173>）に対しパリティスイート `--project=new` の **54 件が green**（職務経歴 10 プロジェクト・製作物 3 件を含む）              |
| 外部画像の描画                                  | 同スイートの「外部画像がすべて描画される」が green。新規 10 バッジを含む全画像が `naturalWidth > 0`。ブラウザーの実測でも壊れた画像 0 件・console エラー 0 件 |
| 製作物の任意 `article`                          | 実 DOM で `portfolio` カードの h5 が `URL` / `リポジトリー` / `技術スタック` の 3 つ（「記事」は出ない）、他 2 件は 4 つであることを確認                      |

### `static-page` / `lapras` × `local-dev`（2026-08-07 / version 4）

version 4（資格 `AWS` へ 1 件追加）を同じ写像規則で再生成し、`local-dev` に対して検証した。
写像規則・宣言済み意図的差異（3 件）は version 2 から変えていない。

| 検査                                            | 結果                                                                                                        |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 決定性                                          | `pnpm exec tsx seed/golden-dataset.ts` が fingerprint `978a9784` を出力                                     |
| 現新一致                                        | `pnpm exec tsx seed/phase-b.ts` が `declaredDiffs: 3` で成功。宣言外の差 0 件                               |
| 再生成し忘れの検出                              | `pnpm exec vitest run`（79 件）が green。`seed/phase-b.test.ts` がディスク上の生成物と写像結果の一致を確認  |
| target が実際にゴールデンデータを配信しているか | `local-dev`（<http://localhost:5173>）に対しパリティスイート `--project=new` の **54 件が green**           |
| 資格の構造                                      | 手書き aria スナップショット（`checkAriaQualifications`）が desktop / mobile とも green。分類 4・`AWS` 2 件 |

### 意図的差異（写像で適用したもの）

いずれも `intentional_diffs.may_change`「GitHub の綴りの是正（新側のみ）」で宣言済み。

| 対象                                     | 論理データ                     | 新側                           |
| ---------------------------------------- | ------------------------------ | ------------------------------ |
| `badges.account[github].label`           | `Github`                       | `GitHub`                       |
| `badges.account[github].imageSrc`        | `…logo=Github&logoColor=white` | `…logo=GitHub&logoColor=white` |
| `badges.framework[github-actions].label` | `GithubActions`                | `GitHub Actions`               |

### 未実施の target

`local-production` / `preview` / `production` は未実施。生成物は target をまたいで同一なので、
各 target で意味を持つのは「その target が実際にゴールデンデータを配信しているか」の検証である。

## 未検証・未収録

- LAPRAS プレビューのタイトル・画像本文: `linkpreview.net` の秘密鍵を要する外部レスポンスのため収録しない。
  手動検証が必要な gap として扱う。
- 外部バッジ画像のバイナリ: 外部サービスの応答は可変。URL・alt・配置は後続のパリティ検証で扱う。
